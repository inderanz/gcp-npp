# Application properties
# Spanner configuration (values injected via environment variables)
spanner.projectId=${SPANNER_PROJECT_ID}
spanner.instanceId=${SPANNER_INSTANCE_ID}
spanner.databaseName=${SPANNER_DATABASE_NAME}

# Logging configuration
logging.level.root=INFO
logging.level.org.springframework.web=INFO
logging.level.com.psredemobank=DEBUG

